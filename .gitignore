.DS_Store
*.log
.env
.env.*
!.env.example

# Claude Code. CLAUDE.md, docs/claude-setup.md and .claude/ (rules, skills,
# agents, hooks, settings.json) are tracked on purpose, so every clone and every
# worktree gets the same rules and the same shell guard.
#
# Only what belongs to one machine stays local.
.claude/memory/
.claude/settings.local.json
